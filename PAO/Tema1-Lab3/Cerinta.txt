Exercitiul 1:
a.
Creati o clasa CandyBox, care va con?ine câmpurile private flavor (String) ?i origin (String). Clasa va avea, de asemenea:
- un constructor fara parametri;
- un constructor ce va ini?ializa toate campurile;
- o metoda de tip float getVolume(), care va intoarce valoarea 0.
Adaugati o metoda toString(), care va returna flavor-ul si regiunea de provenien?a a cutiei de bomboane.

b.
Din clasa initiala deriva?i clasele Lindt, Milka, Heidi. Pentru un design interesant, cutiile vor avea forme diferite, cu dimensiuni float:
- Lindt va fi un paralelipiped dreptunghic;
- Milka va fi un cilindru;
- Heidi va fi un cub.
Clasele vor avea:
- constructori fara parametri;
- constructori care permit ini?ializarea membrilor. Identifica?i o modalitate de reutilizare a codului existent. Pentru fiecare tip de cutie veti initializa, in constructor, campurile flavor ?i origin cu tipul corespunzator.
Suprascrieti metoda getVolume() pentru a intoarce volumul specific fiecarei cutii de bomboane.
Suprascrieti metoda toString() în clasele derivate, astfel încat aceasta sa utilizeze implementarea metodei toString() din clasa de baza. Returna?i un mesaj de forma “The ” + origin + “ ” + flavor + “ has volume ” + volume;

c.
Adauga?i o metoda equals() în clasa CandyBox. Justifica?i criteriul de echivalenta ales. 
Hint: Puteti genera automat metoda, cu ajutorul IDE. Selecta?i câmpurile considerate ?i analiza?i în ce fel va fi suprascrisa metoda equals.

d.
Creati o clasa CandyBag, care va con?ine un array cu mai multe cutii din fiecare tip. Pentru obiectele de tip Lindt testa?i egalitatea lor.

e.
Adaugati clasei Milka metoda printMilkaDim(), care va afi?a dimensiunile razei ?i inaltimii. Procedati in mod analog cu celelalte tipuri de cutii, adaugand metodele printHeidiDim() si printLindtDim(), în care sa afi?a?i dimensiunile fiecarei cutii.

